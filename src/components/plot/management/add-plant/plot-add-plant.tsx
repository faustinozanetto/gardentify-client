import { VStack } from '@chakra-ui/react';
import React from 'react';

interface PlotAddPlantProps {}

const PlotAddPlant: React.FC<PlotAddPlantProps> = (props) => {
  return <VStack></VStack>;
};

export default PlotAddPlant;
